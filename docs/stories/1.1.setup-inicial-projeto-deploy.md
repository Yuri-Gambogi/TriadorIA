# Story 1.1: Setup inicial do projeto e deploy

## Status

Approved

## Story

**As a** desenvolvedor (Yuri assistido pelo @dev),
**I want** um projeto Next.js inicializado, conectado ao GitHub e com deploy automático no Vercel,
**so that** toda alteração futura seja publicada automaticamente em produção e o time tenha uma base sólida pra construir as próximas stories do Epic 1.

## Acceptance Criteria

1. Repositório Git inicializado em `Triador-AI/` com `.gitignore` apropriado para Next.js (inclui `.next/`, `node_modules/`, `.env.local`, `coverage/`, etc.)
2. Projeto Next.js 15 (App Router) com TypeScript em modo estrito configurado (`strict: true` + `noUncheckedIndexedAccess: true`)
3. Tailwind CSS 4 instalado e configurado, com Shadcn/ui inicializado e pelo menos 1 componente base funcional (`Button`)
4. Phosphor Icons instalado e renderizando ao menos 1 ícone na página inicial (validar regra "Phosphor Icons, não Lucide")
5. Projeto Vercel conectado ao repositório GitHub com deploy automático na branch `main` (push em `main` resulta em deploy produção)
6. Página inicial `/` renderiza "Triador AI — em construção" com tipografia Inter, dark mode toggle funcional e está acessível em URL pública do Vercel
7. README na raiz do projeto com seções: visão geral (1 parágrafo), pré-requisitos (Node 20, pnpm 9), setup local (passo a passo), comandos disponíveis, link pra `docs/prd.md` e `docs/architecture.md`
8. `pnpm dev` inicia o servidor local em `http://localhost:3000` sem warnings/erros
9. `pnpm build` completa com sucesso (sanity check de build de produção)
10. `pnpm lint` e `pnpm typecheck` executam sem erros

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> O projeto Triador AI ainda não possui `.aios-core/core-config.yaml` configurado. A validação de qualidade nesta story usará processo de revisão manual (Yuri + @dev) baseado nos Acceptance Criteria.
> Para habilitar CodeRabbit no futuro, criar `core-config.yaml` com `coderabbit_integration.enabled: true`.

## Tasks / Subtasks

> **⚠️ Atenção @dev:** Tasks 7 e 8 contêm passos que **Yuri executa manualmente** (criar conta/repo no GitHub, conectar Vercel). Quando chegar nelas, pause e peça pro Yuri executar — não tente fazer sozinho. Ver seção [Ações Manuais do Yuri](#ações-manuais-do-yuri-crítico) abaixo pro passo a passo.

- [ ] **Task 1: Inicializar repositório Git** (AC: 1)
  - [ ] 1.1. Verificar que estamos em `C:\Users\WINDOWS\Documents\Projetos\Triador-AI\`
  - [ ] 1.2. Executar `git init` na pasta raiz
  - [ ] 1.3. Configurar `.gitignore` padrão Next.js (gerado automaticamente pelo `create-next-app`; complementar com `.env.local`, `*.log`, `coverage/`, `.vercel/`)
  - [ ] 1.4. Criar branch `main` como default
  - [ ] 1.5. Primeiro commit: `chore: initial commit [Story 1.1]`

- [ ] **Task 2: Bootstrap do projeto Next.js 15** (AC: 2)
  - [ ] 2.1. Executar `pnpm dlx create-next-app@latest .` na raiz com flags: `--typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --turbopack`
  - [ ] 2.2. Confirmar estrutura: `app/`, `public/`, `tsconfig.json`, `next.config.ts`, `tailwind.config.ts` (ou configuração inline do Tailwind 4)
  - [ ] 2.3. Editar `tsconfig.json` para garantir `"strict": true`, `"noUncheckedIndexedAccess": true`, `"noImplicitOverride": true`
  - [ ] 2.4. Verificar versão do React: 19.x (Next 15 já traz)
  - [ ] 2.5. Criar `.nvmrc` com `20`

- [ ] **Task 3: Configurar Tailwind 4 + Shadcn/ui** (AC: 3)
  - [ ] 3.1. Validar que Tailwind 4 foi instalado (vem com `create-next-app@latest` em versão recente)
  - [ ] 3.2. Inicializar Shadcn/ui: `pnpm dlx shadcn@latest init` — escolher: New York style, Slate base color, CSS Variables = yes
  - [ ] 3.3. Adicionar componente Button: `pnpm dlx shadcn@latest add button`
  - [ ] 3.4. Adicionar componente Toggle ou modo escuro via `next-themes`: `pnpm add next-themes`
  - [ ] 3.5. Criar `components/theme-provider.tsx` envolvendo `ThemeProvider` do next-themes
  - [ ] 3.6. Atualizar `app/layout.tsx` envolvendo children com `<ThemeProvider>`

- [ ] **Task 4: Configurar Phosphor Icons** (AC: 4)
  - [ ] 4.1. Instalar: `pnpm add @phosphor-icons/react`
  - [ ] 4.2. Importar 1 ícone na home (ex: `import { Sparkle } from '@phosphor-icons/react/dist/ssr'`) usando o sub-path `dist/ssr` pra Server Components
  - [ ] 4.3. Renderizar com `<Sparkle size={32} weight="bold" />` próximo ao título
  - [ ] 4.4. Banir `lucide-react` via ESLint em `eslint.config.js` usando regra `no-restricted-imports`:
    ```js
    // eslint.config.js — adicionar dentro do array de configs
    {
      rules: {
        'no-restricted-imports': ['error', {
          paths: [
            {
              name: 'lucide-react',
              message: 'Triador AI usa Phosphor Icons. Importe de @phosphor-icons/react ou @phosphor-icons/react/dist/ssr.',
            },
          ],
        }],
      },
    }
    ```
  - [ ] 4.5. Confirmar que a regra dispara: criar arquivo temp `app/test-lucide.tsx` com `import { X } from 'lucide-react'`, rodar `pnpm lint` e ver erro. Depois deletar o arquivo de teste.

- [ ] **Task 5: Configurar Inter font e tipografia** (AC: 6)
  - [ ] 5.1. Em `app/layout.tsx`, importar via `next/font/google`: `import { Inter } from 'next/font/google'`
  - [ ] 5.2. Configurar `const inter = Inter({ subsets: ['latin'], display: 'swap', variable: '--font-inter' })`
  - [ ] 5.3. Aplicar `className={inter.variable}` no `<html>` ou `<body>`
  - [ ] 5.4. Configurar `tailwind.config.ts` (ou CSS) com `font-sans: var(--font-inter)`

- [ ] **Task 6: Criar página inicial** (AC: 6)
  - [ ] 6.1. Editar `app/page.tsx` para mostrar:
    - Container centralizado vertical/horizontal
    - Ícone Phosphor `Sparkle` (size 48)
    - Título H1 "Triador AI"
    - Subtítulo H2 cinza "Em construção"
    - Botão Shadcn de toggle de tema (claro/escuro)
  - [ ] 6.2. Garantir responsividade básica (padding ajustando em sm/md/lg)
  - [ ] 6.3. Aplicar paleta documentada na front-end-spec (foreground slate-900, muted slate-500)

- [ ] **Task 7: Conectar ao GitHub [REQUER AÇÕES MANUAIS DO YURI]** (AC: 1, 5)
  - [ ] 7.1. **AÇÃO MANUAL DO YURI:** criar repositório vazio no GitHub (nome sugerido: `triador-ai`). Ele decide se é privado ou público.
  - [ ] 7.2. **AÇÃO MANUAL DO YURI:** copiar URL SSH ou HTTPS do repo
  - [ ] 7.3. Adicionar remote: `git remote add origin <URL>`
  - [ ] 7.4. Push inicial: `git push -u origin main` *(o @dev pede pro Yuri confirmar — Story 1.1 do PRD não menciona @github-devops mas regra do AIOS é delegar push)*

- [ ] **Task 8: Conectar ao Vercel e deploy automático [REQUER AÇÕES MANUAIS DO YURI]** (AC: 5, 6)
  - [ ] 8.1. **AÇÃO MANUAL DO YURI:** acessar https://vercel.com, conectar conta GitHub, importar repositório `triador-ai`
  - [ ] 8.2. **AÇÃO MANUAL DO YURI:** configurar projeto na UI Vercel — framework auto-detectado como Next.js. Sem env vars nesta story (próximas stories adicionam Supabase/Anthropic). Deploy automático em `main` é o padrão.
  - [ ] 8.3. Confirmar primeira build no Vercel completa com sucesso (verificar dashboard)
  - [ ] 8.4. Acessar URL pública gerada (ex: `triador-ai.vercel.app`) e verificar página renderizada
  - [ ] 8.5. Anotar URL no README (na seção "Deploy")

- [ ] **Task 9: Criar README** (AC: 7)
  - [ ] 9.1. Criar `README.md` na raiz com seções:
    - Título e descrição (1 parágrafo do problema + solução)
    - Pré-requisitos (Node 20 LTS, pnpm 9, Git)
    - Setup local (clonar, `pnpm install`, `pnpm dev`)
    - Comandos disponíveis (`pnpm dev`, `pnpm build`, `pnpm lint`, `pnpm typecheck`)
    - Documentação (links para `docs/prd.md`, `docs/architecture.md`, `docs/front-end-spec.md`)
    - Deploy (URL Vercel)
    - Stack resumido (1 linha cada: Next.js 15, Tailwind 4, Shadcn, Supabase planejado, Claude API planejado)

- [ ] **Task 10: Configurar scripts e validação local** (AC: 8, 9, 10)
  - [ ] 10.1. Verificar que `package.json` tem scripts: `dev`, `build`, `start`, `lint`
  - [ ] 10.2. Adicionar script `typecheck`: `"typecheck": "tsc --noEmit"`
  - [ ] 10.3. Executar localmente `pnpm dev`, `pnpm build`, `pnpm lint`, `pnpm typecheck` e confirmar zero warnings/erros
  > **Nota:** Setup de Husky + lint-staged + commitlint foi movido pra Story 1.2 como tarefa firme (não-opcional). Justificativa: Story 1.1 já tem muito setup; pré-commit hooks fazem mais sentido com Vitest configurado.

- [ ] **Task 11: Validação final e commit** (todos AC)
  - [ ] 11.1. Rodar checklist mental pelos 10 ACs: cada um marca ✓
  - [ ] 11.2. Commit final: `feat: setup inicial Next.js 15 + Tailwind + Shadcn + Phosphor + Vercel [Story 1.1]`
  - [ ] 11.3. Push pra `main` e confirmar deploy automático funcionando
  - [ ] 11.4. Atualizar status da story para `Review` e notificar Yuri

## Dev Notes

> **CRÍTICO:** Esta seção contém TUDO que o @dev precisa pra executar a Story 1.1 sem ler os 4 documentos completos. Toda fonte está citada. Se algo crítico está faltando, sinalize antes de começar.

### Contexto do Projeto

- **Projeto:** Triador AI — primeiro projeto greenfield pessoal de Yuri Gambogi
- **Localização local:** `C:\Users\WINDOWS\Documents\Projetos\Triador-AI\`
- **Plataforma de Yuri:** Windows 11 + Git Bash. Comandos devem funcionar nesse ambiente. Yuri NUNCA programou antes — qualquer ação manual (criar conta, conectar repo) deve ter passo a passo claro.
- **Esta é a primeira story do Epic 1.** Não há story anterior pra contexto.

### Stack Definitiva (não substituir)

[Source: architecture.md#tech-stack]

| Categoria | Tecnologia | Versão |
|-----------|------------|--------|
| Frontend Language | TypeScript | 5.6+ (estrito) |
| Frontend Framework | Next.js | 15.x (App Router) |
| UI Library | React | 19.x |
| UI Components | Shadcn/ui | latest |
| CSS Framework | Tailwind CSS | 4.x |
| Iconografia | **Phosphor Icons** | 2.x (REGRA: nunca Lucide) |
| Package Manager | pnpm | 9.x |
| Node.js | LTS | 20.x |
| Build Tool | Next.js (Turbopack) | nativo |
| Linter | ESLint | 9.x (flat config) |
| Formatter | Prettier | 3.x |
| Hosting | Vercel | — |

### Convenções Críticas

[Source: architecture.md#coding-standards]

- **TypeScript estrito:** `"strict": true`, `"noUncheckedIndexedAccess": true`. NUNCA usar `any` — usar `unknown` + narrowing.
- **Server Components por padrão:** App Router. Só vire `'use client'` quando precisar de hooks/interatividade.
- **Conventional Commits:** `tipo(escopo): mensagem [Story X.Y]`. Ex: `feat: setup inicial [Story 1.1]`. Tipos: `feat`, `fix`, `chore`, `docs`, `refactor`, `test`, `style`.
- **Ícones:** SOMENTE Phosphor. Banir Lucide via README/ESLint. (Regra do produto)

### Estrutura de Pastas Esperada (Snapshot Mínimo desta Story)

[Source: architecture.md#unified-project-structure]

Após Story 1.1, a estrutura mínima esperada:

```
Triador-AI/
├── .github/                       # Vazio nesta story (workflows vêm em Story 1.x futura)
├── app/
│   ├── layout.tsx                 # Root layout com ThemeProvider + Inter font
│   ├── page.tsx                   # Página "Triador AI — em construção"
│   ├── globals.css                # Tailwind + CSS variables Shadcn
│   └── favicon.ico
├── components/
│   ├── theme-provider.tsx         # Wrapper next-themes
│   └── ui/
│       └── button.tsx             # Shadcn Button (referência)
├── lib/                           # Vazio nesta story; popula nas próximas
├── public/
│   └── (favicon, etc.)
├── docs/
│   ├── prd.md
│   ├── architecture.md
│   ├── front-end-spec.md
│   └── stories/
│       └── 1.1.setup-inicial-projeto-deploy.md
├── .env.example                   # Pode criar vazio nesta story; populado em 1.2
├── .gitignore
├── .nvmrc                         # "20"
├── README.md
├── package.json
├── pnpm-lock.yaml
├── tsconfig.json
├── next.config.ts
├── tailwind.config.ts             # ou CSS inline (Tailwind 4 mudou padrão)
├── eslint.config.js
└── postcss.config.mjs
```

> Pastas como `supabase/`, `tests/`, `stores/`, `lib/db/`, `lib/engine/` NÃO devem ser criadas nesta story. Elas surgem nas stories que as utilizam.

### Comandos Esperados

[Source: architecture.md#development-workflow]

```bash
pnpm install                  # instala deps
pnpm dev                      # inicia dev (porta 3000)
pnpm build                    # build de produção
pnpm start                    # roda build local
pnpm lint                     # ESLint
pnpm typecheck                # tsc --noEmit
```

### Princípios de Design da Home (UX)

[Source: front-end-spec.md#branding-style-guide e #visual-identity]

- **Tipografia:** Inter (única fonte do projeto), pesos 400/500/600/700 — carregar via `next/font/google` para zero CLS
- **Paleta inicial relevante a esta story:**
  - Background light: `#FFFFFF`
  - Background dark: `#0B1120`
  - Foreground light: `#0F172A` (slate-900)
  - Foreground dark: `#F8FAFC` (slate-50)
  - Muted: `#64748B` (slate-500)
  - Primary (futuro CTA): `#E74C3C` (vermelho-laranja queimado — NÃO usar nesta story; reservado pra Story 1.4)
- **Dark mode:** suportado desde o MVP via `next-themes` (toggle defaults pra `system`)
- **Type scale relevante:**
  - H1: `text-4xl font-bold leading-tight` (36px)
  - H2: `text-3xl font-bold leading-tight` (30px)
- **Spacing:** sistema Tailwind padrão (4px base)
- **Border radius:** `rounded-md` (6px) pra botões, `rounded-lg` (8px) pra cards

### Phosphor Icons — Padrão de Uso

[Source: front-end-spec.md#iconography]

- Import em Server Components: `import { Sparkle } from '@phosphor-icons/react/dist/ssr'`
- Import em Client Components: `import { Sparkle } from '@phosphor-icons/react'`
- Weights: `regular` (default), `bold`, `fill`, `duotone`
- Tamanhos canônicos: 16, 20, 24, 32, 48
- Para esta story, usar `Sparkle` (weight bold, size 32 ou 48) próximo ao título "Triador AI"

### Ações Manuais do Yuri (CRÍTICO)

Esta story envolve passos que precisam que o Yuri execute MANUALMENTE fora do terminal (não tem como o @dev fazer automaticamente sem credenciais):

1. **Criar repositório no GitHub** (Task 7.1):
   - Acessar https://github.com/new
   - Nome sugerido: `triador-ai`
   - Privacidade: decisão do Yuri (recomendado privado no MVP)
   - **Não inicializar com README** (já vamos criar local)
   - Copiar URL após criar

2. **Conectar ao Vercel** (Task 8.1):
   - Acessar https://vercel.com (criar conta se ainda não tiver — pode usar conta GitHub)
   - "Add New" → "Project" → selecionar `triador-ai` da lista
   - Manter configurações padrão (framework auto-detectado)
   - Clicar "Deploy"
   - Aguardar build (~2-3 min)
   - Anotar URL gerada (algo como `triador-ai-yuri.vercel.app`)

3. **Push inicial pro remote** (Task 7.4):
   - Comando: `git push -u origin main`
   - Pode pedir autenticação GitHub via browser (Git Credential Manager faz isso no Windows)

> **Regra AIOS:** push remoto seria responsabilidade do `@github-devops`, mas como é setup inicial e Yuri tá envolvido, faz junto com `@dev`. Próximas stories devem usar `@github-devops` pra push.

### Variáveis de Ambiente

[Source: architecture.md#environment-configuration]

Nesta Story 1.1 **não há env vars necessárias** (sem Supabase, sem Anthropic ainda). Criar arquivo `.env.example` VAZIO ou com comentário "Populado em Story 1.2 (Supabase) e Story 2.1 (Anthropic)".

Não comitar `.env.local`.

### Considerações de Segurança

- `.env.local` no `.gitignore` (já vem do create-next-app)
- Sem secrets nesta story
- Verificar que `node_modules/`, `.next/`, `.vercel/`, `coverage/` estão no `.gitignore`

### Testing

[Source: architecture.md#testing-strategy]

**Nesta Story 1.1 não há testes a serem criados** — não há lógica nem componentes não-triviais. A próxima story (1.2 ou 1.3) deve introduzir Vitest + Playwright e criar primeiro teste.

**Validação manual (sanity check):**

- [ ] `pnpm dev` sobe servidor em http://localhost:3000 sem warnings/erros
- [ ] Página inicial mostra "Triador AI", ícone Sparkle, toggle de tema
- [ ] Toggle de tema alterna entre claro/escuro corretamente
- [ ] `pnpm build` completa sem erros
- [ ] `pnpm lint` zero erros (warnings aceitáveis se justificáveis)
- [ ] `pnpm typecheck` zero erros
- [ ] Deploy Vercel funciona — URL pública renderiza a página

**Padrão de testes futuro (apenas referência):**
- Testes unitários e de componentes: `tests/unit/` usando Vitest + React Testing Library
- Testes E2E: `tests/e2e/` usando Playwright
- Setup do Vitest e Playwright virá na Story 1.x (sugestão: incluir em Story 1.2 junto com Supabase)

### Critérios de "Pronto" desta Story

A story 1.1 estará pronta quando:

1. Todos os 10 Acceptance Criteria atendidos
2. Todas as 11 Tasks marcadas como concluídas
3. Validação manual rodou (lista acima)
4. Yuri conseguiu acessar a URL Vercel e ver a página
5. Status atualizado para `Review`
6. Yuri valida e aprova → status `Done`

### Próximos passos após esta story

- **Story 1.2** (próxima): Setup do Supabase + schema inicial + RLS — depende desta estar Done
- **Story 1.3:** Autenticação Email + Google
- **Story 1.4:** Tela de captura por texto
- **Story 1.5:** Tela de visualização da triagem salva

## Change Log

| Date       | Version | Description                                        | Author |
|------------|---------|----------------------------------------------------|--------|
| 2026-06-01 | 0.1     | Criação inicial da Story 1.1 pelo @sm River        | River  |
| 2026-06-01 | 0.2     | Aplicadas 3 melhorias do *story-checklist: regra ESLint firme banindo Lucide; Husky movido pra Story 1.2; sinalização visível das tasks com ação manual do Yuri | River  |
| 2026-06-01 | 0.3     | Status mudado de Draft → Approved pelo Yuri. Pronta pra entrega ao @dev. | River  |

## Dev Agent Record

> *Esta seção será populada pelo `@dev` durante a implementação.*

### Agent Model Used
*A preencher pelo @dev*

### Debug Log References
*A preencher pelo @dev*

### Completion Notes List
*A preencher pelo @dev*

### File List
*A preencher pelo @dev*

## QA Results

> *Esta seção será populada pelo `@qa` (ou pelo próprio Yuri no MVP) após a story estar em status `Review`.*
