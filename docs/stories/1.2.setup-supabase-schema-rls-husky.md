# Story 1.2: Setup do Supabase + schema inicial + RLS + Husky

## Status

Approved

## Story

**As a** desenvolvedor (Yuri assistido pelo @dev),
**I want** o Supabase configurado com tabelas iniciais (`profiles`, `triagens`, `itens`) + Row-Level Security ativo + Drizzle ORM tipado + Husky/commitlint pra padronizar commits,
**so that** as próximas stories (1.3 auth, 1.4 captura, 1.5 visualização) tenham fundação sólida de banco + ferramentas de qualidade pra impedir commits ruins desde o início.

## Acceptance Criteria

1. Conta Supabase criada e projeto Triador AI inicializado (com região `us-east-1` pra alinhar com Vercel iad1)
2. Arquivo `.env.local` populado com 4 variáveis: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`
3. Arquivo `.env.example` criado com placeholders das 4 variáveis (versionado no Git)
4. Migration SQL inicial em `supabase/migrations/{timestamp}_init.sql` cria:
   - Extensão `uuid-ossp`
   - Enums: `plan_tier` (free/pro), `status_triagem` (pendente/classificando/classificada/erro), `bucket_type` (prioridade/roi_alto/delega/depois/descarta)
   - Tabela `profiles` (estende auth.users com `display_name`, `plan`, `criterios_personalizados`, `onboarding_completed`, `stripe_customer_id`)
   - Tabela `triagens` (id, user_id FK, texto_bruto, imagem_url, status, tokens_input, tokens_output, custo_estimado_usd, modelo_usado, created_at, classified_at)
   - Tabela `itens` (id, triagem_id FK, texto, bucket, justificativa, posicao, concluido, concluido_at, gatilho_retorno, delegacao, created_at)
   - Trigger `handle_new_user` que cria `profile` automaticamente após signup em `auth.users`
   - Índices: `idx_profiles_stripe_customer`, `idx_triagens_user_created`, `idx_triagens_status` (parcial), `idx_itens_triagem`, `idx_itens_bucket`, `idx_itens_concluido` (parcial)
5. Row-Level Security ativo nas 3 tabelas com policies que isolam por `auth.uid()`:
   - `profiles`: select/update própriio
   - `triagens`: select/insert/update/delete próprio
   - `itens`: select/insert/update/delete via FK ao dono da triagem
6. Migration aplicada com sucesso no Supabase remoto (verificável no Supabase Studio)
7. Drizzle ORM instalado e `lib/db/schema.ts` definido com TODAS as 3 tabelas + enums (mirror das migrations) — `pnpm db:generate` rodando sem erros
8. `lib/db/client.ts` exporta instância Drizzle conectada ao Supabase via `DATABASE_URL`
9. `@supabase/ssr` instalado e `lib/auth/server.ts` exporta:
   - `getServerSupabase()` — cliente Supabase pra Server Components/Route Handlers
   - `getServerSession()` — pega usuário atual ou null
   - `requireUser()` — pega usuário ou lança erro UNAUTHENTICATED
10. `middleware.ts` na raiz refresca sessão Supabase em toda request autenticada
11. Scripts `pnpm` adicionados: `db:generate`, `db:migrate`, `db:push`, `db:studio`
12. Husky + lint-staged + commitlint instalados e configurados:
    - Pre-commit hook: `pnpm lint` + `pnpm typecheck` (bloqueia commit se falhar)
    - Commit-msg hook: validar Conventional Commits (`type(scope): message [Story X.Y]`)
13. `pnpm typecheck`, `pnpm lint`, `pnpm build` continuam passando (zero regressão)
14. Página inicial (`/`) continua renderizando sem erros (sanity check de não-regressão)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Projeto Triador AI ainda não tem `.aios-core/core-config.yaml`. Validação manual com base nos ACs.

## Tasks / Subtasks

> **⚠️ Atenção @dev:** Tasks 1 e 2 contêm **ações manuais do Yuri** (criar conta Supabase, criar projeto, copiar credenciais). Quando chegar nelas, **pause e instrua o Yuri** — não tente fazer sozinho. Ver seção [Ações Manuais do Yuri](#ações-manuais-do-yuri-crítico) abaixo pra passo a passo.

- [ ] **Task 1: Criar projeto Supabase [REQUER AÇÃO MANUAL DO YURI]** (AC: 1)
  - [ ] 1.1. Yuri acessa https://supabase.com e cria conta (se ainda não tem). Pode logar com GitHub pra ser mais rápido.
  - [ ] 1.2. Yuri clica "New Project" → escolhe Organization → preenche:
    - Name: `triador-ai`
    - Database Password: gerar forte (Supabase oferece botão "Generate password") — **Yuri salva num lugar seguro** (ex: gerenciador de senhas, ou anota; será usado no `DATABASE_URL`)
    - Region: **East US (Virginia) — `aws-us-east-1`** (mesma região do Vercel iad1)
    - Pricing Plan: **Free** (suficiente pro MVP)
  - [ ] 1.3. Aguarda ~2 minutos enquanto Supabase provisiona o projeto

- [ ] **Task 2: Coletar credenciais e configurar `.env.local` [REQUER AÇÃO MANUAL DO YURI]** (AC: 2, 3)
  - [ ] 2.1. Dex cria `.env.example` na raiz com placeholders (sem segredos)
  - [ ] 2.2. Yuri, no Supabase Dashboard do projeto novo, acessa:
    - **Settings → API**: copia `Project URL`, `anon public` key, `service_role` key
    - **Settings → Database**: copia `Connection string` (URI) — substitui `[YOUR-PASSWORD]` pela senha salva na Task 1.2
  - [ ] 2.3. Dex cria `.env.local` (gitignored) com as 4 vars preenchidas. **Yuri cola as 4 strings copiadas quando Dex pedir.**

- [ ] **Task 3: Instalar dependências** (AC: 7, 8, 9)
  - [ ] 3.1. Drizzle ORM stack: `pnpm add drizzle-orm postgres && pnpm add -D drizzle-kit` (driver é `postgres.js`, não `pg` — `@types/pg` é desnecessário)
  - [ ] 3.2. Supabase SSR: `pnpm add @supabase/ssr @supabase/supabase-js`
  - [ ] 3.3. Confirmar que `package.json` tem as 4 novas deps + 2 dev deps

- [ ] **Task 4: Criar migration SQL inicial** (AC: 4, 5)
  - [ ] 4.1. Criar pasta `supabase/migrations/`
  - [ ] 4.2. Criar arquivo `supabase/migrations/{YYYYMMDDHHMMSS}_init.sql` com schema completo (ver [Migration SQL](#migration-sql-completa) no Dev Notes). **`{YYYYMMDDHHMMSS}` é placeholder** — use timestamp atual do momento da execução, ex: `20260602153045_init.sql`
  - [ ] 4.3. Aplicar migration no Supabase remoto via Supabase SQL Editor (Dashboard → SQL Editor → New Query → cola SQL → Run). **Alternativa:** via Supabase CLI (mais complexo pro MVP, pular)
  - [ ] 4.4. Verificar no Supabase Studio (Table Editor) que as 3 tabelas existem com colunas corretas
  - [ ] 4.5. Verificar Authentication → Policies que RLS está ativo nas 3 tabelas

- [ ] **Task 5: Setup Drizzle ORM** (AC: 7, 8, 11)
  - [ ] 5.1. Criar `drizzle.config.ts` na raiz com config apontando pra `lib/db/schema.ts` + `DATABASE_URL`
  - [ ] 5.2. Criar `lib/db/schema.ts` com tipos Drizzle equivalentes ao SQL (ver [Drizzle Schema](#drizzle-schema-libdbschemats) no Dev Notes)
  - [ ] 5.3. Criar `lib/db/client.ts` exportando instância Drizzle conectada via `postgres()` driver
  - [ ] 5.4. Adicionar scripts ao `package.json`:
    ```json
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
    ```
  - [ ] 5.5. **NÃO rodar `pnpm db:generate` nesta story.** Em vez disso, validar Drizzle assim:
    - Rodar `pnpm db:studio` — abre Drizzle Studio em http://localhost:4983 mostrando as 3 tabelas do Supabase (provando que o `lib/db/schema.ts` está alinhado com o banco)
    - **Política nesta fase:** SQL no `supabase/migrations/` é fonte de verdade pro schema; Drizzle TS apenas gera tipos. Migrations sempre escritas manualmente em SQL. (Em story futura, talvez Story 2.x, podemos transicionar pra `db:generate` + `db:migrate` como fluxo padrão.)
    - Se o `db:studio` mostrar inconsistência (ex: coluna no banco que não está no TS), atualizar `lib/db/schema.ts` manualmente até bater

- [ ] **Task 6: Setup Supabase Auth helpers** (AC: 9, 10)
  - [ ] 6.1. Criar `lib/auth/server.ts` com:
    - `getServerSupabase()`: cria cliente via `@supabase/ssr` `createServerClient` + cookies do Next
    - `getServerSession()`: chama `supabase.auth.getUser()` e retorna user ou null
    - `requireUser()`: chama `getServerSession`; se null, lança `ApiError('UNAUTHENTICATED', ...)` — também criar `lib/api/error-handler.ts` mínimo com a classe `ApiError`
  - [ ] 6.2. Criar `middleware.ts` na raiz do projeto com refresh de sessão Supabase em rotas que não sejam estáticas/api/webhook
  - [ ] 6.3. Criar `lib/api/error-handler.ts` mínimo (classe ApiError + tipos básicos). Implementação completa do `withErrorHandler` virá na Story que tiver primeiro endpoint (Story 1.4 quando criar POST /api/triagens)

- [ ] **Task 7: Setup Husky + lint-staged + commitlint** (AC: 12)
  - [ ] 7.1. `pnpm add -D husky lint-staged @commitlint/cli @commitlint/config-conventional`
  - [ ] 7.2. `pnpm exec husky init` (cria `.husky/` com hook básico)
  - [ ] 7.3. Editar `.husky/pre-commit` pra rodar `pnpm lint-staged` (em vez do default `pnpm test`)
  - [ ] 7.4. Criar `.husky/commit-msg` com `pnpm exec commitlint --edit "$1"`
  - [ ] 7.5. Adicionar config `lint-staged` ao `package.json`:
    ```json
    "lint-staged": {
      "*.{ts,tsx}": ["eslint --fix", "tsc --noEmit --project tsconfig.json"],
      "*.{json,md,css}": ["prettier --write"]
    }
    ```
  - [ ] 7.6. Instalar Prettier se ainda não tiver: `pnpm add -D prettier eslint-config-prettier`
  - [ ] 7.7. Criar `commitlint.config.js` na raiz com config conventional + regra customizada exigindo `[Story X.Y]` no footer (opcional — pode adiar pra story futura)
  - [ ] 7.8. Testar fazendo um commit ruim (ex: `git commit -m "teste"`) — deve falhar no commit-msg hook
  - [ ] 7.9. Fazer commit válido (`feat: setup supabase + drizzle + husky [Story 1.2]`) pra confirmar fluxo
  > **🚨 Saída de emergência se hook travar:** se algum hook do Husky bloquear um commit legítimo e o @dev precisar desbloquear pra investigar, usar `git commit --no-verify -m "..."` pula TODOS os hooks. Uso de emergência apenas; depois investigar a causa raiz do bloqueio.

- [ ] **Task 8: Atualizar `.gitignore` e arquivos auxiliares** (AC: 3)
  - [ ] 8.1. Confirmar que `.env.local` está no `.gitignore` (já vem do create-next-app)
  - [ ] 8.2. Adicionar `drizzle/` ao `.gitignore` se Drizzle gerar arquivos que não devem ir pro repo (alternativamente, commitar `drizzle/` pra ter histórico de migrations geradas — decidir caso a caso; recomendado **commitar** pra ter rastreabilidade)
  - [ ] 8.3. Atualizar README com nova seção "Banco de dados" mencionando Supabase + Drizzle + como rodar migrations

- [ ] **Task 9: Validação local** (AC: 13, 14)
  - [ ] 9.1. `pnpm typecheck` — zero erros
  - [ ] 9.2. `pnpm lint` — zero erros
  - [ ] 9.3. `pnpm build` — completa
  - [ ] 9.4. `pnpm dev` + abrir http://localhost:3000 — página inicial renderiza igual antes (não-regressão)
  - [ ] 9.5. Confirmar que `pnpm db:studio` abre o Drizzle Studio (porta 4983) e mostra as 3 tabelas

- [ ] **Task 10: Commit final + push** (todos AC)
  - [ ] 10.1. Checklist mental dos 14 ACs
  - [ ] 10.2. Commit conventional: `feat: setup supabase + drizzle + auth helpers + husky [Story 1.2]`
  - [ ] 10.3. Push pra `main` (Vercel re-deploya com env vars novas — mas elas precisam ser adicionadas no Vercel separadamente; ver [Vercel Env Vars](#vercel-env-vars-deploy) no Dev Notes)
  - [ ] 10.4. **Yuri adiciona as 4 env vars no Vercel Dashboard manualmente** (Settings → Environment Variables) — porque Vercel não lê `.env.local` (que é só local)
  - [ ] 10.5. Trigger redeploy no Vercel (ou simplesmente push) e confirmar que produção ainda funciona
  - [ ] 10.6. Status story → `Ready for Review`

## Dev Notes

> **CRÍTICO:** Esta seção contém TUDO que o @dev precisa pra executar a Story 1.2 sem ler 4 documentos completos. Toda fonte está citada.

### Contexto

- **Story anterior (1.1):** Done. Next.js 16 + Tailwind 4 + Shadcn + Phosphor + Inter rodando local + Vercel. Repositório: https://github.com/Yuri-Gambogi/TriadorIA. Produção: https://triador-ia.vercel.app.
- **Esta story (1.2):** adiciona a camada de dados (Supabase) e a primitive de autenticação (helpers + middleware). NÃO implementa UI de login (vem na 1.3).
- **Husky/commitlint adicionados aqui** — foram movidos da Story 1.1 (decisão registrada no checklist da Story 1.1 v0.2).

### Stack desta Story (não substituir)

[Source: architecture.md#tech-stack]

| Categoria | Tecnologia | Versão |
|-----------|------------|--------|
| Database | PostgreSQL (Supabase) | 16 |
| ORM | Drizzle ORM | latest |
| Postgres driver | postgres.js | latest |
| Auth | Supabase Auth | nativo |
| Auth helpers | @supabase/ssr | latest |
| Git Hooks | Husky | latest |
| Pre-commit | lint-staged | latest |
| Commit Lint | @commitlint/cli + @commitlint/config-conventional | latest |
| Formatter | Prettier | latest |

### Variáveis de Ambiente

[Source: architecture.md#environment-configuration]

**`.env.example`** (versionado, sem secrets):
```bash
# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL="https://xxxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGc..."
SUPABASE_SERVICE_ROLE_KEY="eyJhbGc..."

# --- Database (Drizzle direct connection) ---
DATABASE_URL="postgresql://postgres:[password]@db.xxx.supabase.co:5432/postgres"
```

**`.env.local`** (gitignored, com secrets reais — Yuri preenche):
- Mesma estrutura, valores reais copiados do Supabase Dashboard.

### Migration SQL completa

[Source: architecture.md#database-schema — versão reduzida pra Story 1.2; pgvector, feedback_loop e storage bucket virão em stories posteriores]

Caminho do arquivo: `supabase/migrations/20260602000000_init.sql` (usar timestamp atual)

```sql
-- ============================================================
-- 20260602000000_init.sql
-- Initial schema for Triador AI (Story 1.2)
-- Tables: profiles, triagens, itens
-- ============================================================

-- Extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Enums
CREATE TYPE plan_tier AS ENUM ('free', 'pro');
CREATE TYPE bucket_type AS ENUM ('prioridade', 'roi_alto', 'delega', 'depois', 'descarta');
CREATE TYPE status_triagem AS ENUM ('pendente', 'classificando', 'classificada', 'erro');

-- profiles (extensão de auth.users)
CREATE TABLE public.profiles (
  id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email text NOT NULL,
  display_name text,
  plan plan_tier NOT NULL DEFAULT 'free',
  criterios_personalizados jsonb,
  onboarding_completed boolean NOT NULL DEFAULT false,
  stripe_customer_id text UNIQUE,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_profiles_stripe_customer ON profiles(stripe_customer_id);

-- Trigger pra criar profile após signup
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS trigger AS $$
BEGIN
  INSERT INTO public.profiles (id, email, display_name)
  VALUES (NEW.id, NEW.email, COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1)));
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE PROCEDURE public.handle_new_user();

-- triagens
CREATE TABLE public.triagens (
  id uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  texto_bruto text,
  imagem_url text,
  status status_triagem NOT NULL DEFAULT 'pendente',
  tokens_input integer NOT NULL DEFAULT 0,
  tokens_output integer NOT NULL DEFAULT 0,
  custo_estimado_usd numeric(10, 6) NOT NULL DEFAULT 0,
  modelo_usado text,
  created_at timestamptz NOT NULL DEFAULT now(),
  classified_at timestamptz,
  CHECK (texto_bruto IS NOT NULL OR imagem_url IS NOT NULL)
);
CREATE INDEX idx_triagens_user_created ON triagens(user_id, created_at DESC);
CREATE INDEX idx_triagens_status ON triagens(status) WHERE status IN ('pendente', 'classificando');

-- itens
CREATE TABLE public.itens (
  id uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  triagem_id uuid NOT NULL REFERENCES triagens(id) ON DELETE CASCADE,
  texto text NOT NULL,
  bucket bucket_type NOT NULL,
  justificativa text NOT NULL,
  posicao integer NOT NULL DEFAULT 0,
  concluido boolean NOT NULL DEFAULT false,
  concluido_at timestamptz,
  gatilho_retorno jsonb,
  delegacao jsonb,
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_itens_triagem ON itens(triagem_id);
CREATE INDEX idx_itens_bucket ON itens(triagem_id, bucket, posicao);
CREATE INDEX idx_itens_concluido ON itens(triagem_id) WHERE concluido = false;

-- ============================================================
-- ROW-LEVEL SECURITY
-- ============================================================
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE triagens ENABLE ROW LEVEL SECURITY;
ALTER TABLE itens ENABLE ROW LEVEL SECURITY;

-- profiles
CREATE POLICY "profiles_select_own" ON profiles FOR SELECT USING (auth.uid() = id);
CREATE POLICY "profiles_update_own" ON profiles FOR UPDATE USING (auth.uid() = id);

-- triagens
CREATE POLICY "triagens_select_own" ON triagens FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "triagens_insert_own" ON triagens FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "triagens_update_own" ON triagens FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "triagens_delete_own" ON triagens FOR DELETE USING (auth.uid() = user_id);

-- itens (via FK ao dono da triagem)
CREATE POLICY "itens_select_own" ON itens FOR SELECT
  USING (EXISTS (SELECT 1 FROM triagens t WHERE t.id = itens.triagem_id AND t.user_id = auth.uid()));
CREATE POLICY "itens_insert_own" ON itens FOR INSERT
  WITH CHECK (EXISTS (SELECT 1 FROM triagens t WHERE t.id = itens.triagem_id AND t.user_id = auth.uid()));
CREATE POLICY "itens_update_own" ON itens FOR UPDATE
  USING (EXISTS (SELECT 1 FROM triagens t WHERE t.id = itens.triagem_id AND t.user_id = auth.uid()));
CREATE POLICY "itens_delete_own" ON itens FOR DELETE
  USING (EXISTS (SELECT 1 FROM triagens t WHERE t.id = itens.triagem_id AND t.user_id = auth.uid()));
```

### Drizzle Schema (`lib/db/schema.ts`)

[Source: architecture.md#data-models adaptado para Drizzle ORM]

```typescript
import { pgTable, uuid, text, timestamp, boolean, integer, jsonb, numeric, pgEnum, index, check } from 'drizzle-orm/pg-core';
import { sql } from 'drizzle-orm';

export const planTier = pgEnum('plan_tier', ['free', 'pro']);
export const bucketType = pgEnum('bucket_type', ['prioridade', 'roi_alto', 'delega', 'depois', 'descarta']);
export const statusTriagem = pgEnum('status_triagem', ['pendente', 'classificando', 'classificada', 'erro']);

export const profiles = pgTable('profiles', {
  id: uuid('id').primaryKey(),
  email: text('email').notNull(),
  displayName: text('display_name'),
  plan: planTier('plan').notNull().default('free'),
  criteriosPersonalizados: jsonb('criterios_personalizados'),
  onboardingCompleted: boolean('onboarding_completed').notNull().default(false),
  stripeCustomerId: text('stripe_customer_id').unique(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
}, (table) => ({
  stripeCustomerIdx: index('idx_profiles_stripe_customer').on(table.stripeCustomerId),
}));

export const triagens = pgTable('triagens', {
  id: uuid('id').primaryKey().default(sql`uuid_generate_v4()`),
  userId: uuid('user_id').notNull(),
  textoBruto: text('texto_bruto'),
  imagemUrl: text('imagem_url'),
  status: statusTriagem('status').notNull().default('pendente'),
  tokensInput: integer('tokens_input').notNull().default(0),
  tokensOutput: integer('tokens_output').notNull().default(0),
  custoEstimadoUsd: numeric('custo_estimado_usd', { precision: 10, scale: 6 }).notNull().default('0'),
  modeloUsado: text('modelo_usado'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  classifiedAt: timestamp('classified_at', { withTimezone: true }),
}, (table) => ({
  userCreatedIdx: index('idx_triagens_user_created').on(table.userId, table.createdAt),
}));

export const itens = pgTable('itens', {
  id: uuid('id').primaryKey().default(sql`uuid_generate_v4()`),
  triagemId: uuid('triagem_id').notNull().references(() => triagens.id, { onDelete: 'cascade' }),
  texto: text('texto').notNull(),
  bucket: bucketType('bucket').notNull(),
  justificativa: text('justificativa').notNull(),
  posicao: integer('posicao').notNull().default(0),
  concluido: boolean('concluido').notNull().default(false),
  concluidoAt: timestamp('concluido_at', { withTimezone: true }),
  gatilhoRetorno: jsonb('gatilho_retorno'),
  delegacao: jsonb('delegacao'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
}, (table) => ({
  triagemIdx: index('idx_itens_triagem').on(table.triagemId),
  bucketIdx: index('idx_itens_bucket').on(table.triagemId, table.bucket, table.posicao),
}));

export type Profile = typeof profiles.$inferSelect;
export type NewProfile = typeof profiles.$inferInsert;
export type Triagem = typeof triagens.$inferSelect;
export type NewTriagem = typeof triagens.$inferInsert;
export type Item = typeof itens.$inferSelect;
export type NewItem = typeof itens.$inferInsert;
```

> **Nota:** o `userId` em `triagens` não tem `.references(() => authUsers.id)` no Drizzle porque `auth.users` é uma tabela gerenciada pelo Supabase em outro schema. A FK existe no SQL mas o Drizzle não precisa modelá-la. RLS no banco já garante isolamento.

### Drizzle Client (`lib/db/client.ts`)

```typescript
import { drizzle } from 'drizzle-orm/postgres-js';
import postgres from 'postgres';
import * as schema from './schema';

const connectionString = process.env.DATABASE_URL;
if (!connectionString) {
  throw new Error('DATABASE_URL is not set');
}

// Disable prepare for Supabase Pooler compatibility
const client = postgres(connectionString, { prepare: false });

export const db = drizzle(client, { schema });
```

### Drizzle Config (`drizzle.config.ts`)

```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './lib/db/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
  verbose: true,
  strict: true,
});
```

### Auth Server Helpers (`lib/auth/server.ts`)

[Source: architecture.md#auth-architecture]

```typescript
import { cookies } from 'next/headers';
import { createServerClient } from '@supabase/ssr';
import { ApiError } from '@/lib/api/error-handler';

export async function getServerSupabase() {
  const cookieStore = await cookies();
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (toSet) => {
          try {
            toSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            );
          } catch {
            // Server Components não podem setar cookies — ignorar.
            // Middleware cuidará do refresh.
          }
        },
      },
    }
  );
}

export async function getServerSession() {
  const supabase = await getServerSupabase();
  const { data: { user } } = await supabase.auth.getUser();
  return user;
}

export async function requireUser() {
  const user = await getServerSession();
  if (!user) {
    throw new ApiError('UNAUTHENTICATED', 'Sessão expirada. Faça login novamente.', 401);
  }
  return user;
}
```

### Error Handler mínimo (`lib/api/error-handler.ts`)

```typescript
export class ApiError extends Error {
  constructor(
    public code: string,
    message: string,
    public status: number,
    public details?: Record<string, unknown>
  ) {
    super(message);
    this.name = 'ApiError';
  }
}

// withErrorHandler completo virá na Story 1.4 quando criar primeiro endpoint
```

### Middleware (`middleware.ts` na raiz)

[Source: architecture.md#protected-route-pattern]

```typescript
import { createServerClient } from '@supabase/ssr';
import { NextResponse, type NextRequest } from 'next/server';

export async function middleware(request: NextRequest) {
  let response = NextResponse.next({ request });

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => request.cookies.getAll(),
        setAll: (toSet) => {
          toSet.forEach(({ name, value, options }) => {
            request.cookies.set(name, value);
          });
          response = NextResponse.next({ request });
          toSet.forEach(({ name, value, options }) => {
            response.cookies.set(name, value, options);
          });
        },
      },
    }
  );

  // Refresh session — necessário pra cookies não expirarem
  await supabase.auth.getUser();

  return response;
}

export const config = {
  matcher: [
    // Pular: _next, api/stripe/webhook (será futuro), arquivos estáticos
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
};
```

> **Nota:** Story 1.3 vai adicionar lógica de redirect (`if (!user && isAppRoute) redirect /login`). Nesta story, middleware só refresca a sessão.

### Vercel Env Vars (Deploy)

**IMPORTANTE:** Vercel NÃO lê `.env.local` (que é só ambiente local de dev). Yuri precisa adicionar as 4 env vars manualmente no Dashboard:

1. Acessa https://vercel.com → projeto `triador-ia` → Settings → Environment Variables
2. Adiciona cada uma das 4 vars (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`)
3. Marca cada uma pra **Production**, **Preview** e **Development**
4. Salva
5. Trigger redeploy (manual ou via novo push)

### Ações Manuais do Yuri (CRÍTICO)

1. **Criar conta + projeto Supabase** (Task 1) — ~5 min
   - https://supabase.com → New Project → preencher form
   - Anotar: URL, anon key, service role key, password do DB

2. **Copiar 4 credenciais e passar pro Dex** (Task 2) — ~2 min
   - Supabase Dashboard → Settings → API → copia URL + anon + service_role
   - Settings → Database → Connection string → URI → substitui `[YOUR-PASSWORD]`
   - Cola tudo no chat com Dex (Dex cria `.env.local` com elas)

3. **Aplicar migration via SQL Editor** (Task 4.3) — ~1 min
   - Supabase Dashboard → SQL Editor → New Query → cola SQL completo da migration → Run
   - Confirmar mensagem "Success"

4. **Adicionar 4 env vars no Vercel** (Task 10.4) — ~3 min
   - Vercel Dashboard → projeto → Settings → Environment Variables
   - Adicionar cada uma das 4 (mesmas que estão no .env.local) marcando Production + Preview + Development

### Configurações de pacote

**`package.json` final esperado** (deps relevantes desta story):
```json
{
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@supabase/ssr": "...",
    "@supabase/supabase-js": "...",
    "drizzle-orm": "...",
    "next": "16.2.7",
    "next-themes": "0.4.6",
    "postgres": "...",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@commitlint/cli": "...",
    "@commitlint/config-conventional": "...",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/pg": "...",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "drizzle-kit": "...",
    "eslint": "^9",
    "eslint-config-next": "16.2.7",
    "eslint-config-prettier": "...",
    "husky": "...",
    "lint-staged": "...",
    "prettier": "...",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
```

**Husky `commitlint.config.js`** (raiz):
```javascript
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
```

### Testing

[Source: architecture.md#testing-strategy]

**Esta Story 1.2 não introduz Vitest/Playwright** — deixei pra Story 1.3 ou 1.4 quando aparecer código testável (auth handlers, route handlers). Aqui só temos:

- **Validação manual:**
  - [ ] `pnpm typecheck` ✓
  - [ ] `pnpm lint` ✓
  - [ ] `pnpm build` ✓
  - [ ] `pnpm dev` ✓ (página inicial renderiza)
  - [ ] `pnpm db:studio` abre Drizzle Studio mostrando 3 tabelas
  - [ ] Supabase Studio: 3 tabelas criadas, RLS ativo (verificável na UI)
  - [ ] Commit ruim (sem conventional) deve falhar no commit-msg hook

### Critérios de "Pronto"

A story 1.2 estará pronta quando:
1. Todos os 14 ACs atendidos
2. Todas as 10 Tasks marcadas como concluídas
3. Validação manual rodou (lista acima)
4. Yuri confirmou que Vercel produção continua funcionando após adicionar env vars
5. Status `Ready for Review` → `Done` (Yuri aprova)

### Próximos passos após esta story

- **Story 1.3** Auth Email + Google: usa `lib/auth/server.ts` + `middleware.ts` criados aqui pra implementar telas de login/cadastro/logout
- **Story 1.4** Captura por texto: usa `lib/db/client.ts` + `lib/db/schema.ts` pra criar primeira Route Handler `/api/triagens`
- **Story 1.5** Visualizar triagem salva: lê do banco e renderiza

## Change Log

| Date       | Version | Description                                              | Author |
|------------|---------|----------------------------------------------------------|--------|
| 2026-06-02 | 0.1     | Criação inicial da Story 1.2 pelo @sm River              | River  |
| 2026-06-02 | 0.2     | Aplicadas 4 melhorias do *story-checklist: (1) Drizzle não roda db:generate; SQL é fonte de verdade; (2) timestamp da migration documentado como placeholder; (3) removido `@types/pg` desnecessário; (4) adicionada saída de emergência `--no-verify` se hook do Husky travar | River |
| 2026-06-02 | 0.3     | Status mudado de Draft → Approved pelo Yuri. Pronta pra entrega ao @dev. | River |

## Dev Agent Record

### Agent Model Used
*A preencher pelo @dev*

### Debug Log References
*A preencher pelo @dev*

### Completion Notes List
*A preencher pelo @dev*

### File List
*A preencher pelo @dev*

## QA Results

> *Esta seção será populada pelo `@qa` (ou Yuri no MVP) após status `Ready for Review`.*
